PORT=8095
VITALS_REALTIME_GATEWAY_SECRET=

# Lower latency: TCP_NODELAY on all sockets (small SSE frames flush sooner).
REALTIME_TCP_NODELAY=

# SSE comment ping interval (ms). 12000 keeps streams warm under proxies; 0 disables pings.
REALTIME_SSE_PING_MS=